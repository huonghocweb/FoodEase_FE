import React from 'react';
import UserFormList from './UserFormList';

const UserFormPage = () => {
    return (
        <div>
            <UserFormList />
        </div>
    );
};

export default UserFormPage;